GMXReply · CUMULATIVE HANDOFF · 2026-03-02

SOURCE OF TRUTH
- This pack was built strictly from Bac 2.zip.
- Arcade files were merged from GMXReply_Bac3_Arcade_REACT_VITE_PHASE53_MASTER_READY only where needed for the React+Vite games route.

WHAT THIS PACK CHANGES
1) Extension pivoted to SAFE MODE
- No X DOM injection.
- No content scripts on x.com.
- Manifest is now copy-only: no buttons inside X, no programmatic insert, no synthetic typing.
- Popup now does:
  - Copy GM
  - Best GM
  - Copy GN
  - Best GN
  - manual clipboard only
  - Open site
  - Open X
  - Connect @handle (stores local extension token)
  - local Reset
- Best buttons use a small local heuristic over a generated batch, then copy the strongest item.
- Public copy works even without login.
- Old extension/content.js and extension/site_sync.js can stay in the folder, but manifest no longer uses them.

2) Arcade merged into the React+Vite site
- Added /arcade route in frontend/src/main.tsx.
- Added React+Vite ArcadePage and the full arcade component/feature trees from Phase 53.
- Added public/arcade.js for the temporary legacy bridge/runtime scaffold used by ArcadePage.
- Added an Open Arcade button in the bridge header and overview notes in frontend/src/App.tsx.
- Added backend routes:
  - /arcade
  - /arcade/*
  Both now serve the bridge build shell.

IMPORTANT DEV NOTE
- Main guaranteed path right now is npm run dev.
- In dev, /arcade works from Vite source immediately.
- On the backend static build path, /arcade serves public/bridge/index.html. If you want the backend-only static route to reflect the new arcade source, you still need a fresh frontend build + sync later.

WHAT WAS NOT DONE YET
- No side panel / hotkeys for the new safe extension yet.
- No Supabase live wiring yet (still one later pass, as planned).
- No Render deploy wiring yet.
- No rebuild of public/bridge static assets in this pack.

FILES IN THIS PACK
- extension/manifest.json
- extension/popup.html
- extension/popup.css
- extension/popup.js
- frontend/src/main.tsx
- frontend/src/App.tsx
- frontend/src/styles.css
- frontend/src/pages/ArcadePage.tsx
- frontend/src/components/arcade/*
- frontend/src/features/arcade/*
- public/arcade.js
- index.js

HOW TO APPLY
1. Unzip this pack over your current project root.
2. Reload the Chrome extension.
3. Open the extension popup:
   - test Copy GM / Copy GN
   - test Best GM / Best GN
   - test Connect @handle
4. Run npm run dev.
5. Open:
   - http://127.0.0.1:5173/bridge
   - http://127.0.0.1:5173/arcade
6. Confirm Arcade page opens and extension is copy-only.

NEXT BEST STEP IF CHAT DIES
- Continue from this pack.
- First verify safe extension popup flow.
- Then do one focused pass for:
  1. static frontend build sync for /arcade on backend
  2. site session/connect/reset cleanup
  3. site i18n cleanup
  4. then one unified Supabase wiring pass
  5. then Render deploy wiring
