GMXReply Phase 2 patch
Date: 2026-02-28

What this patch does
1) Converts the React bridge from an internal tab control panel into a route-based shell under /bridge.
2) Makes Referrals the first-class React business screen at /bridge/referrals.
3) Keeps Admin available at /bridge/admin.
4) Keeps all other main product surfaces explicitly on legacy /app until they are migrated one by one.
5) Re-includes the fixed TopNav without react-router-dom so this patch stays self-contained.

Files included
- frontend/src/App.tsx
- frontend/src/components/TopNav.tsx
- frontend/src/styles.css
- GMXReply_PHASE2_REACT_BRIDGE_HANDOFF_2026-02-28.txt

Expected result
- /bridge shows React home overview
- /bridge/referrals shows the React Referrals page
- /bridge/admin shows the React Admin page
- /bridge/gm, /bridge/gn, /bridge/themes, /bridge/leaderboard, /bridge/extension-themes, /bridge/upgrade-pro show intentional placeholders that point users back to /app
- legacy /app remains untouched and is still the source of truth for the rest of the product

Why this is the right next step
- It stops React from being just a dev tab toy
- It starts the real screen-by-screen migration path
- It keeps the backend stable
- It avoids another round of random legacy referral DOM hotfixes

Next step after this patch
- Phase 3 should deepen Referrals in React (copy, KPI blocks, promoter details, right-side logic parity if needed)
- After Referrals is stable, move Admin deeper or centralize business gating helpers shared by Referrals/Admin
