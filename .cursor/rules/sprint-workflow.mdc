---
description: GMXReply refactor sprint rules — one step at a time, no drift
alwaysApply: true
---

# Sprint workflow (GMXReply refactor)

## Order of work

1. **One sprint step at a time** — do not start the next step until the current step is fully done, tested, synced, and documented.
2. **No rushed patches** — every change must match project conventions; read surrounding code before editing.
3. **Zero parity drift** — after client/static edits: sync `public/` → `frontend/public/` and `public/bridge/` where applicable; run `verify:parity` and `audit:runtime`.
4. **Describe each step** — in commits and chat: what was wrong, what changed, how verified.

## Definition of done (per step)

- Root cause fixed (not masked)
- Tests/audits relevant to the step pass (`npm test`, `test:suite`, `audit:all` as appropriate)
- All mirrors byte-identical where required
- `docs/IDEAS_AND_TODO.md` or sprint tracker updated if the step closes a listed item
- Push + `verify:prod` only when the user expects deploy for that step
- **Agent pushes commits** — after each sprint step is done locally, push to `origin/main` yourself (retry on transient network errors); then run `verify:prod` and report `build=` id

## Sprint 1 queue (strict order)

1. Arcade cover `imageUrl` fixes (wrong covers)
2. ru/uk Arcade i18n corruption
3. Remove duplicate `coverSrc()` in arcade.js
4. Stop accidental `bridge/app.html` copy
5. Wire `mountLineListSkeleton` in bankui

Do not begin step N+1 until step N meets definition of done.

## Sprint 2 queue (strict order)

1. ru/uk extension popup `ext_*` i18n corruption
2. `arcade-covers.json` madalin-stunt-cars wrong URL
3. Mark VPS deploy scripts obsolete (Render-only)
4. Docs drift: `PLAN.md`, `REPO_BREAKDOWN.md` refresh

## Sprint 3 queue (strict order) — site + extension stability

Supabase deferred until site and extension are stable (`DB_MODE=sqlite` on Render).

1. Referral progress bar — i18n label + reward hint (`ref_progress_meter_html`) — DONE
2. Extension connect/status strings — move hardcoded EN in `popup.js` to i18n — DONE
3. Extension ↔ site session sync audit + tests — DONE
4. Boot bundle inventory (defer implementation until 1–3 done) — DONE

Sprint 3 complete. Bundle 5c–5d complete.

## Sprint 4 queue (strict order) — Arcade

1. Category `.webp` fallback covers — DONE (4.1)
2. Pro checkout flow for Arcade gate — DONE (4.2)

Do not begin step N+1 until step N meets definition of done.

## Sprint 5 queue (strict order) — Arcade covers (continued)

1. Per-game `.webp` covers for LOCAL_GAME_COVERS — DONE (5.1)
2. Catalog static audit + prune orphan local-cover pipeline — DONE (5.2)

Do not begin step N+1 until step N meets definition of done.

## Sprint 6 queue (strict order) — extension unlock parity

1. Extension cosmetics unlock gating — DONE (6.1)

## Sprint 7 queue (strict order) — Arcade hardening

1. Catalog split (`data/arcade-catalog.json` + build) — DONE (7.1)
2. Quick insert (URL / embed / GameDistribution id) — DONE (7.2)
3. Embed URL offline audit in CI — DONE (7.3)
4. SEO `/arcade/:slug` deep-links — DONE (7.4)

## Sprint 8 queue (strict order) — Pro value copy

1. Wallet / Arcade: explicit «Pro unlocks all Arcade games» — DONE (8.1)
2. `prod-verify`: `/arcade/:slug` redirect spot-check — DONE (8.2)

## Sprint 9 queue (strict order) — extension Arcade nudge

1. Game of the Day — daily notification + popup card with live title — DONE (9.1)

## Sprint 10 queue (strict order) — Home conversion (SITE_RECOMMENDATIONS priority #1)

1. Guest demo без регистрации — Try GM/GN on Home via `/api/public/random-bulk` — DONE (10.1)
2. Hero video — 12s SVG loop + optional MP4 hook, CTA to guest demo — DONE (10.2)

## Sprint 11 queue (strict order) — Yearly plan marketing (SITE_RECOMMENDATIONS priority #2)

1. Wallet yearly savings copy + featured y1 card + per-month quote — DONE (11.1)

## Sprint 12 queue (strict order) — Arcade achievements (SITE_RECOMMENDATIONS priority #4)

1. Local achievements panel + launch tracking — DONE (12.1)

## Sprint 13 queue (strict order) — GM/GN quick presets (SITE_RECOMMENDATIONS priority #5)

1. Casual / Professional / Fun one-click tone on GM & GN — DONE (13.1)

## Sprint 14 queue (strict order) — Referral progress bar (SITE_RECOMMENDATIONS priority #7)

1. Span-based progress + need hint + boot cache hydrate — DONE (14.1)

## Sprint 15 queue (strict order) — PWA (SITE_RECOMMENDATIONS priority #8)

1. Manifest + service worker + install button — DONE (15.1)

## Sprint 16 queue (strict order) — Referral share UX

1. Viral hook + one-click copy/share — DONE (16.1)

## Sprint 17 queue (strict order) — Leaderboard UX

1. Top-3 medals + persistent your-rank strip — DONE (17.1)

## Sprint 18 queue (strict order) — Themes UX

1. Hover preview + Dark/Light/Colorful groups + Pro unlocks all hint — DONE (18.1)

## Sprint 19 queue (strict order) — GM/GN history + shortcuts

1. Batch history + Ctrl+Enter hardening + edit hint banner — DONE (19.1)

## Sprint 20 queue (strict order) — Mobile nav

1. Bottom nav + GM/GN swipe — DONE (20.1)

## Sprint 21 queue (strict order) — Prediction onboarding

1. Newbie intro + external learn-more links — DONE (21.1)

## Sprint 22 queue (strict order) — Extension tab UX

1. Sync hub + UI previews + Chrome Web Store CTA — DONE (22.1)

## Sprint 23 queue (strict order) — Upgrade Pro compare

1. Free vs Pro comparison table + testimonial — DONE (23.1)

## Sprint 24 queue (strict order) — Home social proof

1. Connected-today counter from public stats API — DONE (24.1)

## Sprint 25 queue (strict order) — SEO meta

1. Unique meta descriptions + og:image for Home, Arcade, Upgrade — DONE (25.1)

## Sprint 26 queue (strict order) — Navigation UX

1. Home > section breadcrumbs on deep tabs — DONE (26.1)

## Sprint 27 queue (strict order) — Performance

1. Preload Arcade page on tab/link hover — DONE (27.1)

## Sprint 28 queue (strict order) — Loading UX

1. Skeleton loaders for GM, GN, Arcade lists — DONE (28.1)

## Sprint 29 queue (strict order) — Offline shell

1. Service Worker static cache + offline /app navigation — DONE (29.1)

## Sprint 30 queue (strict order) — SEO content

1. Blog guides (Top 10 .io games + GM replies) — DONE (30.1)

## Sprint 31 queue (strict order) — Extension retention

1. Game of the Day card + toast polish — DONE (31.1)

Do not begin step N+1 until step N meets definition of done.
