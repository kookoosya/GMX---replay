GMXReply — HANDOFF R67 — GM/GN + ARCADE SOURCE FIX

Что исправлено

1. Починен backend bulk generation для GM/GN
- index.js теперь использует curated ordinary banks для classic/min/mid/max
- source для ordinary bank взят из пользовательского txt со списками GM/GN
- generate-bulk теперь стабильно добирает до count=40
- saveRecent и getRecentRows зажаты защитой, чтобы db/write сбои не валили endpoint 500
- bulk route имеет fallback вместо server crash

2. Починен min cleanup после кривого R66-pass
- убраны битые regex с control-char вместо \b
- min снова режется предсказуемо
- exact duplicate внутри txt больше не режет count до 39: недостающий слот добивается fallback generation

3. Arcade возвращён к пользовательскому списку игр
- frontend/src/features/arcade/externalPortalCatalog.ts теперь содержит 50 игр из пользовательского txt
- public/bridge/assets/ArcadePage-D7YjbdiP.js тоже переписан на тот же список, чтобы текущий runtime сайта сразу видел 50 карточек без rebuild
- старый GameMonetize mini-list выключен (ne=[]), чтобы не было лишних 2 карточек поверх пользовательского списка

Какие файлы изменены
- Backend/index.js
- Backend/frontend/src/features/arcade/externalPortalCatalog.ts
- Backend/public/bridge/assets/ArcadePage-D7YjbdiP.js

Что проверить локально

Backend:
cd "C:\Users\user\Desktop\GMX - Replay\Backend"
npm run dev

Проверка GM/GN:
- открыть вкладку GM
- открыть вкладку GN
- запрос min/classic/en должен больше не ловить 500 на /api/generate-bulk
- bulk должен отдавать 40 строк
- тексты должны идти из нормального живого банка, а не из поломанного шаблонного мусора

Проверка Arcade:
- открыть arcade route
- карточек должно быть 50
- не должно оставаться старых 2 gamemonetize-карточек сверху
- covers должны тянуться из imageUrl списка
- Load more должен постепенно раскрывать каталог

Что осталось дальше
- отдельно можно пройти ещё один audit-pass по лишним public/bridge/assets файлам, но без rebuild это трогать осторожно
- если потом понадобится полный parity со свежим Vite build, можно отдельно синхронизировать hashed assets после локального build
