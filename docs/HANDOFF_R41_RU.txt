GMXReply — HANDOFF R41 (RU)

Что сделано
- Собран крупный пакет по Themes / Wallpapers / Extension Sync, без дробления на микропатчи.
- Backend/index.js взят из R40: единая раздача /assets и /bridge/assets + cross-origin headers для svg/webp.
- Усилен sync между сайтом и extension popup:
  - Backend/extension/site_sync.js теперь на GMX_FORCE_SITE_SYNC делает реальный syncOnce() и отвечает уже готовыми значениями.
  - Backend/extension/popup.js теперь сразу подхватывает ответ sync в storage перед перерисовкой popup.
- Починена нормализация legacy extension wallpaper id:
  - ext_01..57 -> extv3_01..57
  - ext_free_1/2 -> ext_free_01/02
  - extv3_1..57 -> extv3_01..57
  - lux_ext_* остаются валидными svg-id для совместимости
- Приведена extension wallpaper логика на сайте к одному набору helper-функций:
  - extWallpaperAssetPath()
  - extWallpaperFullUrl()
  - extWallpaperThumbUrl()
- Render extension wallpapers в site app теперь использует единые helper-функции вместо ручной сборки путей.
- setExtView() теперь синкает ext view наружу, а не только меняет localStorage локально.
- applyExtWallpaper() на сайте теперь сохраняет нормализованный id.
- Убран runtime-баг в clear wallpaper handler, где использовался wp.id вне области видимости.
- Изменения зеркально внесены и в Backend/public/app.js, и в Backend/frontend/public/app.js.

Что НЕ сделано
- Не трогал Arcade.
- Не трогал wallet/payments/security этап.
- Не добавлял новые визуальные контролы в popup UI, добивал именно sync/apply логику.
- Не переводил lux_ext_* в основной UI-каталог сайта как новые отдельные карточки, только оставил совместимость.

Что проверить
1) Сайт -> Extension Themes -> Wallpapers:
   - выбрать extv3_01
   - выбрать extv3_20
   - выбрать extv3_57
   - после выбора открыть popup и проверить, что фон реально меняется
2) Сайт -> Extension Themes:
   - переключить Theme / Wall / Custom
   - проверить, что popup подхватывает именно view, а не зависает на старом состоянии
3) Проверить старые сохранённые id:
   - если в storage были ext_01/ext_02 и т.п., popup не должен падать и должен мигрировать к extv3_XX
4) Site wallpapers:
   - clear wallpaper больше не должен ронять JS из-за wp is not defined
5) Smoke:
   - главная жива
   - referrals живы
   - wallet жив
   - popup открывается без syntax/runtime crash

Что делать дальше
- Если popup wallpaper начнёт применяться стабильно, следующий большой пакет = добить remaining lag cleanup + extension visual polish + чистка дублей путей.
- Если всё ещё не применится, нужен уже конкретный popup error log/скрин из DevTools, потому что базовая sync/id/path логика в этом пакете уже закрыта.
