# PATCH 03 Smoke Check

1) Start dev mode as usual.
2) Open the Vite UI.
3) Confirm the previous duplicate-key warnings from `frontend/src/legacy/legacyApp.ts` no longer spam the console.
4) Click through tabs and switch a few languages.
5) Confirm labels still render normally and no tab names/descriptions are missing.
