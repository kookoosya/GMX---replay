Новый чат. Продолжаем GMX Reply/Replay (site + Chrome extension). Формат работы: я нуб, сам код не правлю. Ты сам правишь архив у себя и возвращаешь готовые ZIP-патчи «распаковал поверх — работает».

ТЕКУЩЕЕ СОСТОЯНИЕ
- ENV: Windows PowerShell, Node v22
- dev запуск: `npm run dev`
- backend: `http://127.0.0.1:10000`
- Vite: `http://127.0.0.1:5173/`
- site legacy bridge: `http://127.0.0.1:5173/app`
- DB_MODE=supabase
- `/api/health` показывает supabase configured/active
- usage и referrals уже частично работают через Supabase
- billing / payment intents / verify ещё не доведены до финального безопасного состояния и не полностью перенесены в Supabase

ЧТО УЖЕ БЫЛО СДЕЛАНО
- Добавлялись backend-патчи на crash safety / health / session stability / billing security, но их нужно считать промежуточными и проверять по факту в живом коде
- Был неудачный i18n-патч, который сломал `frontend/src/legacy/legacyApp.ts`
- Последний безопасный шаг: rollback `frontend/src/legacy/legacyApp.ts` из исходного архива, чтобы снова поднять сайт без Vite syntax error

ПОСЛЕДНЯЯ РЕАЛЬНАЯ ПРОБЛЕМА
- Vite падал с ошибкой:
  `frontend/src/legacy/legacyApp.ts:6925:3: ERROR: Expected "}" but found ";"`
- Нужно продолжать только от рабочего состояния, не ломая фронт

ЧТО НУЖНО ДЕЛАТЬ ДАЛЬШЕ
1) Сначала проверить, что после rollback фронт снова запускается и UI открывается
2) Затем сделать нормальный аудит текущего `index.js` и понять, какие backend-патчи реально применены и не конфликтуют ли они
3) Потом уже двигаться по приоритету:
   - payments / pro / limits / secure verify
   - premium themes/wallpapers pipeline
   - extension stability / languages / messaging
   - cleanup архива и подготовка к git / Render

ПРАВИЛА РАБОТЫ
- Без теории и без «сам вставь код»
- Только готовые ZIP-патчи
- Если патч маленький и безопасный, можно давать по 1–3 сразу
- На каждый патч: коротко написать, что реально проверено, а что не проверено
- Если что-то сломали, сначала быстрый safe rollback, потом уже улучшения
