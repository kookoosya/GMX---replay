R74 runtime fixes

Что исправлено
- GM/GN backend generation routes теперь имеют жёсткий fallback и не должны падать в request_failed из-за ranked-ветки или saveRecent
- Extension Themes теперь рендерятся на первом открытии, как обычные Themes
- Vite dev proxy и legacy static runtime синхронизированы с backend origin
- Arcade cards получили local fallback covers вместо битых remote preview
- Добавлены compat redirect paths /bridge/* -> root files

Что проверить
1. /app GM и GN: Quick 1 и Batch 10 больше не должны писать request_failed
2. /arcade.html и /bridge/arcade: у карточек должны появляться локальные картинки, даже если remote cover мёртвый
3. Extension Themes: Theme / Wallpapers / Custom background должны рендериться уже на первом открытии
4. Vite dev: /app на 5173 не должен терять /app.js /mode.js /entitlements.js
