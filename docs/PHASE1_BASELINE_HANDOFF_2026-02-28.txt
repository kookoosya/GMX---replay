GMXReply Phase 1 baseline handoff (2026-02-28)

Baseline used
- Archive available in this chat: Bac 2.zip (the uploaded file name differs from the prompt wording "Bac 2 (2).zip").
- Active git branch inside archive: feature/react-supabase
- Repo remote: https://github.com/kookoosya/GMX---replay.git

What was verified
- Root package.json is the one-command entry and owns dev/start scripts.
- frontend/ is a real React + Vite app, not a blank scaffold.
- Legacy product UI is still primary in production via /app -> public/app.html + public/app.js.
- React currently contains at least Referrals and Admin screens in frontend/src/pages.
- Dev mode: Vite on 5173 runs LegacyApp by default; /bridge path switches to the React panel.
- Before this patch, production had no proper /bridge route and render.yaml expected npm run build even though root package.json had no build script.

Phase 1 changes applied
1) Root build pipeline
- package.json
  - added build:frontend = npm --prefix frontend run build
  - added build = npm run build:frontend && node tools/sync-frontend-build.mjs
- tools/sync-frontend-build.mjs
  - new helper that copies frontend/dist into public/bridge

2) Deploy alignment
- render.yaml
  - branch changed to main (intended stable branch once current work is merged)
  - buildCommand changed to install root + frontend deps, then run root build

3) Production React bridge routing
- frontend/vite.config.ts
  - build base is now /bridge/ during production build, while dev stays /
- index.js
  - added BRIDGE_DIR / BRIDGE_INDEX
  - added sendBridgeIndex helper
  - added GET /bridge exact route before static middleware
  - express.static now uses redirect:false to avoid directory redirect issues
  - added GET /bridge/* fallback after static middleware for SPA deep links

Why this matters
- Legacy /app stays intact.
- React migration can now ship screen-by-screen behind /bridge in production without replacing the main site.
- Render build no longer points to a missing root build script.

Current repo workflow reality
- The archive worktree is NOT clean. There are many modified/untracked files already in the baseline.
- Do not pretend this is a clean branch cut. Commit this patch as a new checkpoint, then normalize flow:
  1. feature/react-supabase (or a new migration branch) -> dev
  2. dev -> main after smoke pass
  3. Render should deploy main only after merge

Recommended next phase
- Phase 2: move React from control-panel shell to first-class migrated screen flow
  1. Referrals: make React route/state the source for that screen, remove remaining legacy dependency for referrals UI
  2. Admin: same for admin tools
  3. Then wire shared nav/router and start replacing legacy tabs one by one

Manual checks after installing deps
- npm install
- npm --prefix frontend install
- npm run build
- npm start
- open /app (legacy should still work)
- open /bridge (React should load from public/bridge)
- deep-link test: /bridge/referrals should still return React index.html
