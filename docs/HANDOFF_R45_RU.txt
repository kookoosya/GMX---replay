GMXReply — HANDOFF R45 (RU)

Что сделано
1) Большой блок referrals visual cleanup для legacy /app.
- Обновлены:
  - public/app.js
  - frontend/public/app.js
  - public/app.css
  - frontend/src/legacy/app.css
  - public/app.html
- Promoter note теперь показывает не только старый unlock text, а живые promoter-метрики:
  - daily limit
  - daily bonus
  - bonus per 20
  - 20-ref chunks
  - promoter active / not active yet
  - next promoter step
- Убран старый misleading label у promo_k_daily: теперь это daily limit, а не base daily.
- Обновлена helper-copy в referrals, чтобы не было старой фразы про "free daily fixed at 70" там, где уже действует promoter bonus.
- Referrals card в legacy /app стала устойчивее визуально:
  - ref status message теперь выглядит как status pill
  - promoter details оформлен как отдельный блок с сеткой KPI и info-cells
  - invited table получила sticky header
  - right help card закрепляется на desktop и нормально падает вниз на smaller widths
  - light mode для referrals card/readability усилен
- public/app.html приведён к тому же refHowCard id, что уже был в React legacy body, чтобы не расходилась стилизация dev/prod путей.

2) Большой блок React bridge referrals cleanup.
- Обновлён frontend/src/pages/ReferralsPage.tsx
- Обновлён frontend/src/styles.css
- В bridge/referrals добавлены:
  - верхняя summary strip с counting rule / owner active / window
  - hero stats grid вместо более рыхлого stat block
  - более стабильный split для referral link + next unlock
  - tableDense layout для invites table
  - tone rows по status/fraud
  - handle теперь виден как @handle
  - reason cell больше не разносит таблицу
- Появились bridge utility styles:
  - badge / badge-ok / badge-warn
  - refHeroGrid / refHeroStat
  - adminHeroStat / adminHeroMeta
  - tableDense / tableCode

3) Большой блок React bridge admin visual cleanup.
- Обновлён frontend/src/pages/AdminPage.tsx
- В admin hero добавлен live workspace summary:
  - recent codes
  - unlock codes
  - paid codes
  - redemptions
  - recent direct grants
  - reward history
  - top eligible now
  - generated now
- Code columns получили truncation class, чтобы длинные коды не ломали таблицы.
- Все admin tables переведены в compact dense spacing.

Что НЕ сделано
- Runtime логика admin endpoints, grants, codes, leaderboard awards не менялась.
- Legacy referrals HTML не переписывался глубоко по структуре, только визуально усилен и синхронизирован с текущей promoter-логикой.
- Полная лингвистическая редактура всех языков в getReferralUiCopy не делалась. Для новых promoter-labels часть fallback остаётся английской, если у локали нет отдельной фразы.
- Full frontend build внутри контейнера не прогонялся, потому что в извлечённой копии не было frontend/node_modules и vite type defs. Вместо этого прогнаны:
  - node --check для public/app.js и frontend/public/app.js
  - TypeScript transpile parse для ReferralsPage.tsx и AdminPage.tsx

Что проверить
1) Legacy /app -> Referrals
- Load stats всё ещё работает
- promoter details показывает daily limit / daily bonus / chunks / next promoter step
- sticky header invited table не ломает scroll
- right help card не уезжает криво на desktop
- light mode referrals читаем

2) React bridge /bridge/referrals
- верхние badges видны и не ломают layout
- hero stats grid не разваливается на узком экране
- next unlock card остаётся читаемой
- invites table не разъезжается от long reason
- row tones видны для fraud / eligible / pending

3) React bridge /bridge/admin
- hero summary виден только когда admin session active
- коды в таблицах не раздувают ширину страницы
- dense tables читаемы и не слипаются

Что делать дальше
Следующий большой блок логично брать такой:
1) support / tools / diagnostics last-copy cleanup
2) access/admin operational polish второго круга по реальным экранам
3) потом уже arcade / migration задачи отдельно, не смешивая с main business UI

Если чат умрёт
- База для продолжения = текущий baseline + cumulative до R45
- Последний пакет изменяет только:
  - public/app.js
  - frontend/public/app.js
  - public/app.css
  - frontend/src/legacy/app.css
  - public/app.html
  - frontend/src/pages/ReferralsPage.tsx
  - frontend/src/pages/AdminPage.tsx
  - frontend/src/styles.css
