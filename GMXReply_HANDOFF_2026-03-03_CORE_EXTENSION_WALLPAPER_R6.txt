GMXReply cumulative handoff — 2026-03-03 — CORE_EXTENSION_WALLPAPER_R6

BASELINE
- Source of truth remains the user archive: Bac 2.zip
- This cumulative pack contains only replacement files.
- Nothing was deleted from the project archive.

WHAT IS INCLUDED
- extension/popup.js
- extension/site_sync.js
- extension/popup.html
- extension/quick.html
- extension/themes.json
- index.js
- public/app.js
- frontend/src/legacy/legacyApp.ts
- public/app.html

WHAT THIS PACK ACTUALLY FIXES
1) public/app.js
- Restores the missing applyExtWallpaper() function.
- This fixes the real runtime error where clicking extension wallpaper on the site could call an undefined function.

2) extension/site_sync.js
- Keeps the immediate reply for GMX_FORCE_SITE_SYNC (no async channel warning loop).
- Adds safe sync for:
  - gmx_ext_theme
  - gmx_theme
  - gmx_ext_wp
  - gmx_ext_view
  - gmx_ext_custom_bg_global
- Keeps anti-churn behavior: storage is not rewritten if values did not actually change.

3) extension/popup.js
- Reads extension appearance keys from storage.
- Applies theme colors from themes.json / remote catalog.
- Applies extension wallpaper/custom background to popup + quick panel through CSS --wall.
- Re-applies visual theme after site sync and after auth/base changes so wallpaper URLs stay correct.

4) extension/popup.html / extension/quick.html
- Keep explicit Open Arcade entry in extension UI.

5) extension/themes.json
- Included because it is already different from baseline and is part of current extension theme behavior.

6) index.js
- Included because it is already different from baseline in the current working tree.
- Current state already has the $80 yearly plan in this file.

7) frontend/src/legacy/legacyApp.ts
- Included because it already contains the applyExtWallpaper() counterpart on the React/Vite legacy side.

8) public/app.html
- Included because it is already different from baseline in the current working tree.

VERIFICATION DONE
- node --check extension/popup.js
- node --check extension/site_sync.js
- node --check public/app.js
- node --check index.js

NOT DONE IN THIS PACK
- No file cleanup / archive cleanup was performed.
- No files were removed, even if they look redundant.
- No Arcade redesign was touched in this pass.
- Generation quality was not fully re-authored from scratch here; index.js is included as the current accumulated state, but deeper tone tuning is still a separate pass.
- Full frontend build was not run here.

IMPORTANT HONEST NOTE
- Some files that had been discussed earlier (like legacyBody/siteI18n mirrors) were not actually changed in the current working tree, so they are intentionally not included in this cumulative pack.
- This pack contains only files that are actually present as changed in the current working copy, plus the real fixes above.

NEXT SAFE STEP
- Apply this cumulative pack over the current Bac 2 working project.
- Then test:
  1) site extension theme/theme view/wallpaper selection
  2) popup + quick panel theme/background rendering
  3) GMX_FORCE_SITE_SYNC from an already-open site tab
- After that, do a separate focused pass on reply-generation quality per mode/style.
