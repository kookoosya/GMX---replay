GMXReply Phase 5 handoff (2026-02-28)

What changed
- Stopped spending time on bridge-shell expansion.
- This patch only touches the React Referrals screen and its styling.
- No backend route changes.
- No new deploy/build workflow changes.

Files in this patch
- frontend/src/pages/ReferralsPage.tsx
- frontend/src/styles.css

What was improved
- Referrals page now shows the real counter split clearly:
  confirmed / active / strict eligible / legacy carryover / eligible / admin credits / effective / link clicks.
- Added 30d / 60d / 90d window switch for invited-user review.
- Added next-unlock progress block with progress bar.
- Added explicit “what counts” explanation tied to backend thresholds from /api/referral/list.
- Added cleaner invited-users table with confirmed time, last insert / last seen, and clearer status tone.
- Fixed the old status coloring issue where confirmed rows were effectively shown like “bad”.

Why this is the right direction
- The task is to finish Referrals first.
- This patch keeps the current bridge as-is and improves the actual migrated business screen instead of adding more shell around it.

What to do next after this patch
1) Replace files from the archive.
2) Run npm run build.
3) Open /bridge/referrals and verify with a real token.
4) If data shape looks good, next patch should be Admin cleanup and then business gating hookup.
