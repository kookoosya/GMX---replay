GMXReply — HANDOFF (RU) — R35 (2026-03-05)

Source of truth:
- Этот архив содержит полный рабочий Backend/ + встроенные assets/ и frontend/ внутри Backend/.
- Больше НЕ нужно держать отдельные assets.zip и frontend.zip рядом — всё уже внутри Backend/.

Что сделано (по фактам):
1) Уменьшил размер архива за счёт удаления дублей ассетов и runtime-мусора.
   - удалены: Backend/.git, Backend/logs, Backend/data.sqlite-wal, Backend/data.sqlite-shm
   - удалены дубли ассетов:
     - Backend/public/bridge/assets/* (заменено на README)
     - Backend/frontend/public/assets/* (заменено на README)

2) Сделал единый source-of-truth для ассетов.
   - Backend/index.js теперь раздаёт:
     - /assets  -> Backend/assets
     - /bridge/assets -> Backend/assets
   Это позволяет НЕ хранить копии ассетов в public/bridge и во frontend/public.

3) Синхронизировал legacy runtime по лимитам/рефералам.
   - Backend/public/app.js:
     - FREE: themes=8, wallpapers=8, extThemes=4, extWallpapers=4
     - Unlock: +1 каждые 3 реферала (первые 8), затем +1 каждые 4
     - Жёсткий лимит массивов: THEMES/WALLPAPERS/EXT_THEMES/EXT_WALLPAPERS -> .slice(0, 60)
     - Обновлены тексты про unlock (убран every 5)

4) Убрал главный источник лагов в теме-галереях (multi-layer background-image).
   - Backend/frontend/src/legacy/legacyApp.ts:
     - deferBg теперь ставит только ОДНУ картинку (без списка fallback-URL)
     - site/ext wallpaper resolver переведён на single-source /assets/... .webp

5) Убрал зависание extension popup при применении обоев.
   - Backend/extension/popup.js:
     - resolveWallpaperSource больше НЕ делает каскад fetch-проверок
     - сразу возвращает URL: {base}/assets/extbg/{id}.webp

Что проверить (smoke):
- npm run dev
- Открыть /app и /bridge
- Themes / Extension Themes:
  - сетка не должна подвисать при первом скролле
  - применение обоев должно происходить без "паузы перед применением"
- Extension popup:
  - переключение wallpaper должно применяться без долгого ожидания

Что осталось (следующий шаг):
- Добавление новых обоев из твоих картинок:
  - нужно положить их в Backend/assets/wallpapers/ и Backend/assets/extbg/
  - после этого обновить списки (если они статические в массиве) или сделать автогенерацию реестра
- Полностью перевести UI превью на real image wallpapers (с mini-thumbs) — сейчас приоритет уже /assets, но thumb-пак пока не введён.

Важно:
- Этот архив уже существенно легче из-за удаления дублей.
- Если где-то в коде ещё всплывает путь /bridge/assets/... — это нормально: он теперь мапится на Backend/assets.
