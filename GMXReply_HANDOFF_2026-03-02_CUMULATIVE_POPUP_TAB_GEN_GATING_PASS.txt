GMXReply cumulative handoff — 2026-03-02

Source of truth used in this session
- Available in container: only GMXReply_CUMULATIVE_PACK_2026-03-02_devloop_tab_popup_gen_fix.zip
- Bac 2.zip was NOT mounted in this session, so all work here was continued from the extracted upper-layer pack in gmx_work

What was already present before this pass
- Popup width/layout was already widened and made scroll-safe
- Last-tab restore was already added in legacy + React wrapper
- Legacy admin HTML was already reduced in public/app.html and frontend/src/legacy/legacyBody.html
- Generation bank had already received a first deeper pass in index.js

What was changed in THIS pass
1) extension/content.js
- Added getUsableEntitlementSnapshot()
- Content script now tries to use a fresh/matching entitlement snapshot before inserting
- If snapshot says the user cannot generate/consume for the selected kind, inline insert is blocked BEFORE text is injected
- This closes the main client-side gating hole where connected users could still insert local list replies and only fail later on apiConsume()
- Added a specific inline toast for mode_locked instead of generic "Could not insert"

2) frontend/src/legacy/legacyApp.ts
- Added pruneLegacyAdminPanels() to the legacy runtime too (it previously existed only in public/app.js)
- Call now runs right after applyLang() during boot
- Result: if any stale legacy admin blocks somehow reappear, selector/FAQ/health cards are hidden again at runtime and titles/notes stay aligned with the reduced admin UI

Business gating audit findings from available files
- extension/background.js already had the stricter local gate for hotkey insert path: it checks entitlement snapshot with helper.canGenerate(...) before sending the insert command
- extension/content.js DID NOT have the same pre-insert gate before this pass; it could insert from local saved lists first and only best-effort call /api/consume afterwards
- That meant connected users could sometimes bypass client-side limit UX on inline insert, especially for list-based inserts
- This pass hardens that path, but server API must still remain the final source of truth
- extension/site_sync.js already clamps cosmetics from site state into extension state via resolveAllowedId(...) and canUseCustomBackground(...)
- extension/content.js inline theme application also resolves theme IDs against stored entitlements before applying
- Popup/site language sync path is already wired through local+sync and queueUiRefresh(); no new delay bug was confirmed from static inspection alone

What is still NOT fully finished
- No full npm run dev end-to-end smoke (full baseline project not mounted here)
- No npm run build smoke (full baseline project not mounted here)
- No full backend/API audit because the cumulative replacement pack does not include the complete project tree or package.json
- Legacy admin runtime still contains old handler code for metrics/selectors/FAQ in JS, but reduced HTML + runtime pruning now keep it out of the visible UI
- If you want a deeper cleanup next, safest next step is to surgically remove those stale admin handler blocks from BOTH public/app.js and frontend/src/legacy/legacyApp.ts (not just hide them)

Recommended next order
1. Real browser smoke-check popup width/scroll on Chrome
2. Real browser check: theme/style changes must not kick back to Home
3. Real browser check: connected free user at limit should now be blocked before inline insert
4. If stable, do one more surgical cleanup pass to delete stale admin JS handler blocks
5. Then continue generation quality pass in index.js if needed

Files that matter in this cumulative pack
- extension/content.js
- frontend/src/legacy/legacyApp.ts
- plus all previously carried replacement files already inside gmx_work
