# Copy to .env.local for local Vite. Prefer 127.0.0.1 (not localhost) on Windows.
# Used by the React app and by vite.config.ts (middleware + proxy) unless GMX_BACKEND_URL overrides.
VITE_API_ORIGIN=http://127.0.0.1:10000
