SAFE_PASS9 HANDOFF 11

What was fixed
1. Removed stale Random 70 UI layer from site source:
- removed hidden gmRand70 / gnRand70 buttons from public/app.html and frontend legacy shell
- removed gmRand70 / gnRand70 bindings from public app runtime copies
- cleaned help copy from "Random 1/10/70" to "Random 1/10"

2. Payment flow cleanup:
- cache-busted app/arcade html to force fresh runtime load instead of old cached R66 scripts
- removed external Buffer CDN dependency from payment flow
- added local browser Buffer fallback + safer PublicKey byte handling
- added explicit insufficient funds checks:
  - insufficient_sol_funds
  - insufficient_token_funds
  - payer_token_account_missing remains explicit

3. Arcade card stability:
- arcade now renders local per-game readable card covers first
- remote image is only used as an upgrade after successful preload
- broken remote image no longer leaves cards in a broken-image state
- arcade html cache-busted so old "approved arcade library" script cache should stop resurfacing

4. Generation cleanup:
- cleaned more cringe GM/GN seed lines in index.js
- strengthened sanitize/scoring against soldier/warrior/goat/star/cutie style drift

What to verify locally
1. Hard refresh /app and /arcade.html after replacing files
2. GM/GN:
- no Random 70 ghost controls
- help text says Random 1/10
3. Payment:
- SOL no-funds should fail honestly
- USDC/USDT with no token balance should say wallet lacks token balance or token account
- old SPL helper error should be gone after hard refresh
4. Arcade:
- cards should show readable per-game local covers immediately
- remote cover can upgrade in if it actually loads
- no broken image placeholders as the primary state

What remains
1. If USDC/USDT now opens wallet but later fails after approval, next pass is verify/on-chain stage only
2. If you want official art for every game, that is a separate per-game cover sourcing pass
3. There is still old drift inside some built bridge assets; current site runtime was cleaned first
