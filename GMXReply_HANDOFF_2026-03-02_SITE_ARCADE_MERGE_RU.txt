GMXReply · SITE ARCADE MERGE HANDOFF · 2026-03-02

БАЗА, ОТ КОТОРОЙ ДЕЛАЛСЯ ЭТОТ ПАК
- source of truth для этого шага: Bac 2.zip
- донор для игр: GMXReply_Bac3_Arcade_REACT_VITE_PHASE53_MASTER_READY.zip
- это cumulative pack ТОЛЬКО для сайта / arcade merge
- extension в этом паке не трогался

ЧТО СДЕЛАНО СЕЙЧАС
1) В сайт добавлен нормальный entry point для Arcade
- в legacy site tabs добавлена вкладка/ссылка "Arcade"
- стоит рядом с "Upgrade Pro"
- клик по Arcade больше НЕ пытается открыть несуществующий internal tab
- вместо этого открывает отдельный route: /arcade

2) Починен клик по tab-strip для нового Arcade entry
- public/app.js и frontend/src/legacy/legacyApp.ts теперь игнорируют .tab без data-tab
- это важно, чтобы ссылка Arcade работала как обычный переход, а не ломала текущий таб и не уводила в Home

3) React + Vite route /arcade реально подключен
- frontend/src/main.tsx теперь умеет грузить ArcadePage при path, который начинается с /arcade
- для /bridge остаётся App
- для всего остального остаётся LegacyApp

4) Влит реальный Arcade React/Vite слой из Phase 53
- добавлен frontend/src/pages/ArcadePage.tsx
- добавлен frontend/src/components/arcade/*
- добавлен frontend/src/features/arcade/*
- включая runtimeAdapter / storage / migrationBundle / supabaseFoundation / gateway scaffold / live merge plan

5) Подключён legacy compat bridge для Arcade
- добавлен public/arcade.js
- добавлен frontend/public/arcade.js
- это нужно, чтобы ArcadePage мог поднимать compat bridge и в backend static режиме, и в Vite dev режиме

6) Добавлены badge assets для Arcade
- добавлены public/assets/badges/*
- добавлены frontend/public/assets/badges/*
- это нужно для bridge/runtime arcade shell

7) Подготовлен Supabase foundation/scaffold слой для Arcade
- добавлены:
  - supabase/04_arcade_progress.sql
  - supabase/05_arcade_runtime_sync_scaffold.sql
  - supabase/06_arcade_runtime_queue_scaffold.sql
  - supabase/07_arcade_runtime_rpc_scaffold.sql
  - supabase/08_arcade_runtime_gateway_stub.sql
- live wiring пока НЕ включён

8) Backend знает route /arcade
- index.js теперь отправляет /arcade и /arcade/* в bridge index shell
- это подготовка под build/static сценарий

ВАЖНОЕ ОГРАНИЧЕНИЕ
- основной рабочий режим всё ещё: npm run dev
- в dev нужно открывать сайт как обычно через Vite (пример: http://127.0.0.1:5173/app)
- тогда /arcade должен открываться сразу
- если открывать backend static route без актуального frontend build, route /arcade может потребовать обычный build sync:
  1. npm --prefix frontend run build
  2. node tools/sync-frontend-build.mjs
- это НЕ основной сценарий, а только smoke/static сценарий

ЧТО НЕ ДЕЛАЛОСЬ В ЭТОМ ПАКЕ
- extension theme sync / reload bug не трогался
- extension Arcade tab / quick panel arcade не делались
- safe copy-only extension не менялся
- site session/connect/reset не трогались
- site i18n глубоко не трогался
- Supabase live auth/runtime wiring НЕ включался

ЧТО ПРОВЕРИТЬ СРАЗУ
1. npm run dev
2. открыть /app
3. в верхних tabs должна быть ссылка Arcade рядом с Upgrade Pro
4. клик по Arcade должен открыть /arcade
5. /arcade должен загрузить React ArcadePage
6. базовый launcher / shelves / modules должны открываться без краша

ЧТО ДЕЛАТЬ ДАЛЬШЕ
1) добить extension theme sync / reload issue
2) решить, нужен ли отдельный Arcade entry внутри extension:
- либо вкладка
- либо кнопка Open Arcade
- либо отдельная секция
3) после стабилизации сделать один нормальный проход по Supabase:
- shared client
- без второго auth контура
- с сохранением local fallback
4) после этого уже готовить Render

КЛЮЧЕВОЕ ПРАВИЛО ДЛЯ СЛЕДУЮЩЕГО ШАГА
- если пользователь приложит более свежий рабочий архив, он станет новым source of truth
- если нет, продолжать от текущего рабочего архива + этого cumulative pack
- ничего не переписывать с нуля
- дальше отдавать только один cumulative zip
